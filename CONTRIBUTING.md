# 参与 Picvero

欢迎提交问题、文案改进和代码修改。反馈商品图问题时，请先去除店铺后台、订单、客户资料和不希望公开的原图内容。

## 本地开发

需要 Python 3.10+。测试、构建与核心运行器使用标准库。

```bash
git clone https://github.com/jidouqie/picvero.git
cd picvero
python3 -m unittest discover -s tests -v
python3 tools/build.py
```

## 修改位置

- `skill_sources/`：主入口与七个快捷入口的指令源。
- `shared/`：各技能共用的说明、模板和本地运行器。
- `tools/`：构建、安装与合成样本回放工具。
- `tests/`：运行记录、文件安全与安装包检查。
- `skills/`：构建产物；修改源文件后重新生成，勿直接修改。
- `project.json`：产品名、前缀、入口和版本号。

涉及运行器或打包时运行全部测试；涉及技能元数据时运行当前环境可用的官方技能格式校验。Pillow 和校验器需要的 PyYAML 都不是核心运行依赖。

## 图片与证据

图片由当前 Codex 会话的内置工具制作。不要加入第三方生成 API 客户端、密钥发现逻辑或作者托管代理。

保留生成、核对、用户选用三个独立状态。未知项仍为未知；生成成功、文件检查通过或合成样本回放都不能当作真实商品还原、平台审核或销量证明。

`work/`、`runs/`、`outputs/` 和个人配置已被忽略。新增公开样例须提供来源与使用许可，并说明哪些画面由 AI 生成。具体授权范围见 [示例素材来源](THIRD_PARTY_NOTICES.md)。

## 提交说明

说明改了什么、解决什么问题，以及实际执行的验证。新增行为请配能检验该行为的测试，保留已有功能与文件安全约束。
